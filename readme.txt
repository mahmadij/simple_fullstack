Just started this app as a fullstack experiment.
You need to install node from https://nodejs.org/en/download/

To start the server, in command line run:

yarn start

The webapp is served up on:

localhost:3000